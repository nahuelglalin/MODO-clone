//
//  PromotionTests.swift
//  MODO-cloneTests
//
//  Created by Nahuel Lalin on 21/02/2024.
//

/*
import XCTest
@testable import Lottie
@testable import MODO_clone

final class PromotionTests: XCTestCase {
    var sut: Promotion!
    var skeletonMock: LottieAnimationView!
    var promotionDto: PromotionDto!
    
    override func setUpWithError() throws {
        let bundle = Bundle(for: type(of: self))
        let nib = UINib(nibName: "Promotion", bundle: bundle)
        sut = nib.instantiate(withOwner: nil, options: nil).first as? Promotion
        promotionDto = PromotionDto(imageUrl: "dummyImageURL", promoUrl: "dummyPromoURL")
    }
    
    override func tearDownWithError() throws {
        sut = nil
        promotionDto = nil
    }
    
    func testInitialization() {
        XCTAssertNotNil(sut)
    }
    
        func testCellConfiguration() {
            // Act
            sut.configure(with: promotionDto)
    
            // Assert
            XCTAssertEqual(sut.image.image, UIImage(named: promotionDto.imageUrl), "Image should be set correctly")
            // You can add more assertions based on your implementation
        }
}
*/
